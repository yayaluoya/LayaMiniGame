/**
 * 文件操作
 */
export default class FileOperation {
    /**
     * 获取文件
     * @param _url 绝对路径
     */
    public static getFile(_url: string): Promise<any> {
        return new Promise<any>((r, e) => { });
    }
}